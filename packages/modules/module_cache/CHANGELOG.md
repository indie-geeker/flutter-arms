## 0.0.1

- Added multi-level cache manager (memory + persistent storage).
- Added cache policies (`normal`, `memoryOnly`, `persistent`, network-aware modes).
- Added cache module wiring for DI lifecycle and statistics support.
