## 0.0.1

- Added `LoggerImpl` with level filtering and multi-output fan-out.
- Added `ConsoleOutput` and rotating `FileOutput` implementations.
- Added simple/JSON formatters and module lifecycle integration.
